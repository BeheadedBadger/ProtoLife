using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelectionPanel : MonoBehaviour
{
    List<string> BuildModeCategories;
    List<string> DemolishModeCategories;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //If another Category is selected, hide the current one and show that one.
        //Hide all Categories if viewmode is selected
    }
}
